<h3><%= action_message %></h3>

<%= form_with(model: [:admins_backoffice, @subject]) do |form| %>
   <div class="form-group">
      <%= form.label :description %>
      <%= form.text_field :description, class:"form-control", placeholder:"Digite o assunto/área"%>
      <% @subject.errors.full_messages_for(:email).each do |message| %>
         <div><span style="color: red;">* <%= message %></span></div>
      <% end %>
   </div>
   <br>
   <button type="submit" class="btn btn-primary"><%= btn %></button>
<% end %>